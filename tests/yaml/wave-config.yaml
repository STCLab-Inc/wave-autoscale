common:
  db_url: sqlite://./tests/db/wave.db

wave_metrics:
  output:
    url: http://localhost:8081/api/metrics-receiver

wave_api_server:
  port: 8081
  host: localhost

wave_web_app:
  port: 3005
  host: localhost
