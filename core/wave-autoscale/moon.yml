language: "rust"
type: "application"

dependsOn:
  - "data-layer"

tasks:
  run:
    command: "cargo watch -x 'run -- -p ./tests/yaml/plan_prometheus_ec2.yaml'"
    inputs:
      - "@globs(sources)"
    env:
      RUST_LOG: info
  build:
    command: "cargo build --release"
    inputs:
      - "@globs(sources)"
    # env:
    #   RUST_LOG: info
