language: "rust"
type: "library"
owners:
  defaultOwner: "@wave-autoscale-team"

tasks:
  # Test - Integration + Unit
  test:
    command: "cargo test -p utils -- --nocapture --test-threads 1"
    inputs:
      - "@globs(sources)"
      - "@globs(tests)"
    env:
      RUST_LOG: debug
